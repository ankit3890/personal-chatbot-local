# Personal Chatbot (OpenAI-only)

Minimal personal chatbot that uses OpenAI for LLM answers and browser TTS for audio output.

## Setup
1. Copy `.env.example` to `.env` and add your `OPENAI_API_KEY`.
2. Install dependencies:
   ```bash
   npm install